%h2 My Funds
.row
  .col-md-12
    = render :partial => 'my_funds_table', :locals => {:buckets => @buckets, :show_actions => 1}

.modal.fade#form-modal{:tabindex => -1, :role => 'dialog', :aria => {:hidden => true}}
