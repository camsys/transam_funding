%li{:class => get_nav_link_class(['funding'])}
  = link_to "#", :class => "dropdown-toggle", :data => {"toggle" => "dropdown"}, :title => 'Funding' do
    %i.fa.fa-usd.fa-2x
    %b.caret
  %ul.dropdown-menu.multi-level{:role => 'menu'}
    %li.text-left.dropdown-submenu
      = link_to funding_sources_path do
        %i.fa.fa-book.fa-fw
        Programs

      %ul.dropdown-menu
        - FundingSource.all.each do |program|
          %li.text-left
            = link_to funding_source_path(program) do
              %i.fa.fa-fw.fa-book
              = program

        - if can? :create, FundingSource
          %li.divider
          %li
            = link_to new_funding_source_path do
              %i.fa.fa-plus.fa-fw
              = "Add Funding Program";

    - if can? :read, FundingTemplate
      %li.text-left
        = link_to funding_templates_path do
          %i.fa.fa-fw.fa-tag
          = ' Templates'

      %li.text-left
        = link_to funding_buckets_path do
          %i.fa.fa-fw.fa-legal
          = ' Buckets'

    %li.divider
    %li.text-left
      = link_to my_funds_funding_buckets_path do
        %i.fa.fa-fw.fa-usd
        = 'My Funds'

    %li.divider
    %li.text-left
      = link_to scheduler_index_path do
        %i.fa.fa-fw.fa-calendar
        Fund Projects

    %li.divider
    %li.text-left
      - if can? :read_all, CapitalPlan
        = link_to capital_plans_path do
          %i.fa.fa-fw.fa-list
          Status
      - else
        = link_to capital_plan_path(CapitalPlan.find_by(fy_year: current_planning_year_year, organization_id: @organization_list.first)) do
          %i.fa.fa-fw.fa-list
          Status
