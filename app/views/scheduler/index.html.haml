= render partial: 'user_activity_line_item_filters/form_panel', locals: {show_sort: 1}

.row
  .col-md-12
    #schedules
      = render 'schedules'

.modal.fade#form-modal{:tabindex => -1, :role => 'dialog', :aria => {:hidden => true}}