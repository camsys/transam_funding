.row
  .col-md-6.col-md-offset-3
    %fieldset
      %legend Add Funds
      = render(:partial => 'shared/form_errors', :locals => {:obj => @funding_request}) unless @funding_request.errors.empty?

      = render 'form'